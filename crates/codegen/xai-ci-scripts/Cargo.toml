[package]
license = "Apache-2.0"
name = "xai-ci-scripts"
version = "0.1.0"
edition.workspace = true
description = "Tests for the ci/ shell scripts that no other crate owns"
publish = false

[dev-dependencies]
tempfile = { workspace = true }
